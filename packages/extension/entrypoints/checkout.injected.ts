// MAIN world에 주입되어 page-context 변수에 접근해야 할 때 사용.
// 현재는 자리만 잡아둠.
export default defineUnlistedScript(() => {
  // window.__NEXT_DATA__, window.__INITIAL_STATE__ 등에서 주문 데이터 추출하는
  // 경로가 필요해지면 여기서 구현.
});
